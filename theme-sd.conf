# Vitreum — SD bucket (1366x768). Pick this file if your firmware reports
# that resolution; banner_scale noscale keeps the asset 1:1, no runtime
# rescale cost. See PROJECT_SPEC.md §5 for the full bucket table.
include theme.conf

banner background-sd.png
banner_scale noscale
